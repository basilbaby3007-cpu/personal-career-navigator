# Career Navigator

Personal Career Navigator — backend (FastAPI) + frontend (static HTML).

Run backend:

```bash
cd backend
python -m uvicorn app:app --port 8888
```

Serve frontend (optional):

```bash
cd frontend
python -m http.server 5500
```

Dependencies (backend): see `requirements.txt`.
